import { unpermissifyChildren } from "../unpermissify";
export const frag = (permissiveChildren) => {
    const children = unpermissifyChildren(permissiveChildren);
    return {
        _oceanic: true,
        type: "fragment",
        children: children || [],
    };
};
